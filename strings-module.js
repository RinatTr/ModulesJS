const firstLetter = str => {return str[0];}
const strLength = str => {return str.length();}
const strSplit = str => {return str.split('');}

module.exports = {
  fl: firstLetter,
  strLen: strLength,
  strSp: strSplit,
}
